{
  "configurations": [
    {
      "type": "debugpy",
      "request": "launch",
      "name": "Django Debug",
      "program": "${workspaceFolder}/manage.py",
      "args": ["runserver", "--noreload"],
      "django": true,
      "justMyCode": true,
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
